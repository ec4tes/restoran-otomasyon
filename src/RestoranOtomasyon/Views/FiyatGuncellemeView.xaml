<UserControl x:Class="RestoranOtomasyon.Views.FiyatGuncellemeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RestoranOtomasyon.Views"
             mc:Ignorable="d"
             Background="Transparent">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:BoolToZamColorConverter x:Key="BoolToZamColorConverter"/>
        
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#a6e3a1"/>
            <Setter Property="Foreground" Value="#1e1e2e"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#94e2d5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Dark ComboBox Style -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#45475a"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#585b70"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"/>
                                                <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#cdd6f4" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,0,30,0" IsHitTestVisible="False"/>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" PopupAnimation="Slide">
                                <Border Background="#313244" BorderBrush="#585b70" BorderThickness="1" CornerRadius="6" MaxHeight="200" MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="DarkComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45475a"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#89b4fa"/>
                                <Setter Property="Foreground" Value="#1e1e2e"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Başlık -->
            <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="💰 Toplu Fiyat Güncelleme" FontSize="20" FontWeight="Bold" Foreground="#cdd6f4"/>
                    <TextBlock Text="Tüm ürünlere veya seçili kategoriye yüzdelik zam veya indirim uygulayın" 
                               Foreground="#bac2de" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
            
            <!-- Ayarlar -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Kategori Seçimi -->
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Kategori" Foreground="#cdd6f4" Margin="0,0,0,8"/>
                            <ComboBox ItemsSource="{Binding Kategoriler}"
                                      SelectedItem="{Binding SelectedKategori}"
                                      DisplayMemberPath="Ad"
                                      Style="{StaticResource DarkComboBoxStyle}"
                                      ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}"/>
                            <TextBlock Text="💡 Boş bırakılırsa tüm ürünlere uygulanır" 
                                       Foreground="#6c7086" FontSize="11" Margin="0,5,0,0"/>
                        </StackPanel>
                        
                        <!-- İşlem Tipi -->
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="İşlem Tipi" Foreground="#cdd6f4" Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal">
                                <RadioButton Content="📈 Zam" 
                                             IsChecked="{Binding IsZam}"
                                             Foreground="#a6e3a1" FontSize="14" Margin="0,0,20,0"/>
                                <RadioButton Content="📉 İndirim" 
                                             IsChecked="{Binding IsIndirim}"
                                             Foreground="#f38ba8" FontSize="14"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                    
                    <!-- Yüzde Girişi -->
                    <StackPanel Margin="0,20,0,0">
                        <TextBlock Text="Yüzde (%)" Foreground="#cdd6f4" Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     Text="{Binding Yuzde, UpdateSourceTrigger=PropertyChanged}"
                                     Background="#45475a" 
                                     Foreground="#cdd6f4" 
                                     BorderThickness="0"
                                     Padding="15"
                                     FontSize="20"
                                     FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="%" Foreground="#cdd6f4" FontSize="24" FontWeight="Bold" 
                                       VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </Grid>
                        
                        <!-- Hızlı Seçimler -->
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <TextBlock Text="Hızlı:" Foreground="#6c7086" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <Button Content="5%" Command="{Binding SetYuzdeCommand}" CommandParameter="5"
                                    Background="#45475a" Foreground="#cdd6f4" Padding="12,6" Margin="0,0,5,0"
                                    BorderThickness="0" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button Content="10%" Command="{Binding SetYuzdeCommand}" CommandParameter="10"
                                    Background="#45475a" Foreground="#cdd6f4" Padding="12,6" Margin="0,0,5,0"
                                    BorderThickness="0" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button Content="15%" Command="{Binding SetYuzdeCommand}" CommandParameter="15"
                                    Background="#45475a" Foreground="#cdd6f4" Padding="12,6" Margin="0,0,5,0"
                                    BorderThickness="0" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button Content="20%" Command="{Binding SetYuzdeCommand}" CommandParameter="20"
                                    Background="#45475a" Foreground="#cdd6f4" Padding="12,6" Margin="0,0,5,0"
                                    BorderThickness="0" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button Content="25%" Command="{Binding SetYuzdeCommand}" CommandParameter="25"
                                    Background="#45475a" Foreground="#cdd6f4" Padding="12,6"
                                    BorderThickness="0" Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                    
                    <!-- Önizleme -->
                    <Border Background="#45475a" CornerRadius="8" Padding="15" Margin="0,20,0,0">
                        <StackPanel>
                            <TextBlock Text="📋 Önizleme" Foreground="#cdd6f4" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Etkilenecek Ürün Sayısı" Foreground="#bac2de" FontSize="12"/>
                                    <TextBlock Text="{Binding EtkilenecekUrunSayisi}" Foreground="#89b4fa" FontSize="24" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Örnek: ₺100 →" Foreground="#bac2de" FontSize="12"/>
                                    <TextBlock Text="{Binding OrnekFiyat, StringFormat=₺{0:N2}}" 
                                               Foreground="{Binding IsZam, Converter={StaticResource BoolToZamColorConverter}}" 
                                               FontSize="24" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Uygula Butonu -->
                    <Button Content="✨ Fiyatları Güncelle"
                            Command="{Binding ApplyCommand}"
                            Style="{StaticResource ActionButtonStyle}"
                            HorizontalAlignment="Center"
                            Margin="0,20,0,0"
                            Padding="40,15"/>
                </StackPanel>
            </Border>
            
            <!-- Sonuç Mesajı -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}" 
                    Visibility="{Binding HasResult, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock Text="{Binding ResultMessage}" 
                           Foreground="#a6e3a1"
                           FontSize="16" 
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
