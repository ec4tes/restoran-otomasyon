<UserControl x:Class="RestoranOtomasyon.Views.RaporView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#1e1e2e">
    
    <UserControl.Resources>
        <Style x:Key="RaporTabStyle" TargetType="Button">
            <Setter Property="Background" Value="#45475a"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#89b4fa"/>
                    <Setter Property="Foreground" Value="#1e1e2e"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#a6e3a1"/>
            <Setter Property="Foreground" Value="#1e1e2e"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#94e2d5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
        
        <!-- Dark Theme ComboBox Style -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#45475a"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#585b70"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"/>
                                                <Path Grid.Column="1" 
                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="#cdd6f4"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Margin="12,0,30,0"
                                              IsHitTestVisible="False"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   PopupAnimation="Slide">
                                <Border Background="#313244"
                                        BorderBrush="#585b70"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        MaxHeight="200"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#585b70"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="DarkComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45475a"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#89b4fa"/>
                                <Setter Property="Foreground" Value="#1e1e2e"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Dark Theme DatePicker Style -->
        <Style x:Key="DarkDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Background" Value="#45475a"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#585b70"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <!-- Dark Theme DataGrid Styles -->
        <Style x:Key="DarkDataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#45475a"/>
            <Setter Property="RowBackground" Value="#313244"/>
            <Setter Property="AlternatingRowBackground" Value="#3b3d52"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>
        
        <Style x:Key="DarkDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#45475a"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#585b70"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
        
        <Style x:Key="DarkDataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#585b70"/>
                    <Setter Property="Foreground" Value="#cdd6f4"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style x:Key="DarkDataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45475a"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#585b70"/>
                </Trigger>
                <Trigger Property="AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#3b3d52"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#313244" Padding="15,10">
            <TextBlock Text="📊 Raporlar" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="#cdd6f4"
                       HorizontalAlignment="Center"/>
        </Border>
        
        <!-- Tab Buttons -->
        <Border Grid.Row="1" Background="#181825" Padding="15,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="📅 Günlük Satış" 
                        Command="{Binding SelectRaporTipiCommand}" 
                        CommandParameter="Gunluk"
                        Margin="5"
                        Style="{StaticResource RaporTabStyle}"/>
                <Button Content="🍕 Ürün Bazlı" 
                        Command="{Binding SelectRaporTipiCommand}" 
                        CommandParameter="Urun"
                        Margin="5"
                        Style="{StaticResource RaporTabStyle}"/>
                <Button Content="💳 Ödeme Tipi" 
                        Command="{Binding SelectRaporTipiCommand}" 
                        CommandParameter="Odeme"
                        Margin="5"
                        Style="{StaticResource RaporTabStyle}"/>
                <Button Content="📆 Aylık Rapor" 
                        Command="{Binding SelectRaporTipiCommand}" 
                        CommandParameter="Aylik"
                        Margin="5"
                        Style="{StaticResource RaporTabStyle}"/>
            </StackPanel>
        </Border>
        
        <!-- Content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <Grid Margin="20">
                <!-- Günlük Satış Raporu -->
                <StackPanel Visibility="{Binding SelectedRaporTipi, Converter={StaticResource StringEqualsVisibility}, ConverterParameter=Gunluk}">
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="📅 Günlük Satış Raporu" FontSize="16" FontWeight="Bold" Foreground="#cdd6f4" Margin="0,0,0,15"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBlock Text="Tarih:" Foreground="#bac2de" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <DatePicker SelectedDate="{Binding SelectedTarih}" Width="150"/>
                                <Button Content="🔍 Rapor Getir" 
                                        Command="{Binding LoadGunlukRaporCommand}"
                                        Style="{StaticResource ActionButtonStyle}"
                                        Margin="15,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- Sonuç -->
                    <Border Style="{StaticResource CardStyle}" Visibility="{Binding GunlukRapor, Converter={StaticResource NullToVisibility}}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Toplam Bilgiler -->
                            <Border Grid.Row="0" Grid.Column="0" Background="#45475a" CornerRadius="8" Padding="15" Margin="5">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="Toplam Adisyon" Foreground="#bac2de" FontSize="12"/>
                                    <TextBlock Text="{Binding GunlukRapor.ToplamAdisyon}" Foreground="#a6e3a1" FontSize="28" FontWeight="Bold"/>
                                </StackPanel>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="1" Background="#45475a" CornerRadius="8" Padding="15" Margin="5">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="Toplam Tutar" Foreground="#bac2de" FontSize="12"/>
                                    <TextBlock Text="{Binding GunlukRapor.ToplamTutar, StringFormat=₺{0:N2}}" Foreground="#89b4fa" FontSize="28" FontWeight="Bold"/>
                                </StackPanel>
                            </Border>
                            <Border Grid.Row="0" Grid.Column="2" Background="#45475a" CornerRadius="8" Padding="15" Margin="5">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="Net Tutar" Foreground="#bac2de" FontSize="12"/>
                                    <TextBlock Text="{Binding GunlukRapor.NetTutar, StringFormat=₺{0:N2}}" Foreground="#f9e2af" FontSize="28" FontWeight="Bold"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Ödeme Detayları -->
                            <Border Grid.Row="1" Grid.Column="0" Background="#45475a" CornerRadius="8" Padding="15" Margin="5">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="💵 Nakit" Foreground="#bac2de" FontSize="12"/>
                                    <TextBlock Text="{Binding GunlukRapor.NakitAdisyon, StringFormat={}{0} adet}" Foreground="#cdd6f4" FontSize="14"/>
                                    <TextBlock Text="{Binding GunlukRapor.NakitTutar, StringFormat=₺{0:N2}}" Foreground="#a6e3a1" FontSize="18" FontWeight="Bold"/>
                                </StackPanel>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="1" Background="#45475a" CornerRadius="8" Padding="15" Margin="5">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="💳 Kart" Foreground="#bac2de" FontSize="12"/>
                                    <TextBlock Text="{Binding GunlukRapor.KartAdisyon, StringFormat={}{0} adet}" Foreground="#cdd6f4" FontSize="14"/>
                                    <TextBlock Text="{Binding GunlukRapor.KartTutar, StringFormat=₺{0:N2}}" Foreground="#89b4fa" FontSize="18" FontWeight="Bold"/>
                                </StackPanel>
                            </Border>
                            <Border Grid.Row="1" Grid.Column="2" Background="#45475a" CornerRadius="8" Padding="15" Margin="5">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="🔄 Karışık" Foreground="#bac2de" FontSize="12"/>
                                    <TextBlock Text="{Binding GunlukRapor.KarisikAdisyon, StringFormat={}{0} adet}" Foreground="#cdd6f4" FontSize="14"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding GunlukRapor.KarisikNakitTutar, StringFormat=₺{0:N2}}" Foreground="#a6e3a1" FontSize="12"/>
                                        <TextBlock Text=" + " Foreground="#bac2de" FontSize="12"/>
                                        <TextBlock Text="{Binding GunlukRapor.KarisikKartTutar, StringFormat=₺{0:N2}}" Foreground="#89b4fa" FontSize="12"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            
                            <!-- İndirim -->
                            <Border Grid.Row="2" Grid.ColumnSpan="3" Background="#f38ba8" CornerRadius="8" Padding="10" Margin="5" Opacity="0.8">
                                <TextBlock Foreground="#1e1e2e" FontWeight="Bold" HorizontalAlignment="Center">
                                    <Run Text="Toplam İndirim: "/>
                                    <Run Text="{Binding GunlukRapor.ToplamIndirim, StringFormat=₺{0:N2}}"/>
                                </TextBlock>
                            </Border>
                        </Grid>
                    </Border>
                </StackPanel>
                
                <!-- Ürün Bazlı Rapor -->
                <StackPanel Visibility="{Binding SelectedRaporTipi, Converter={StaticResource StringEqualsVisibility}, ConverterParameter=Urun}">
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="🍕 Ürün Bazlı Satış Raporu" FontSize="16" FontWeight="Bold" Foreground="#cdd6f4" Margin="0,0,0,15"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBlock Text="Başlangıç:" Foreground="#bac2de" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <DatePicker SelectedDate="{Binding BaslangicTarihi}" Width="130"/>
                                <TextBlock Text="Bitiş:" Foreground="#bac2de" VerticalAlignment="Center" Margin="15,0,10,0"/>
                                <DatePicker SelectedDate="{Binding BitisTarihi}" Width="130"/>
                                <Button Content="🔍 Rapor Getir" 
                                        Command="{Binding LoadUrunSatislariCommand}"
                                        Style="{StaticResource ActionButtonStyle}"
                                        Margin="15,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <Border Style="{StaticResource CardStyle}">
                        <DataGrid ItemsSource="{Binding UrunSatislari}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  Style="{StaticResource DarkDataGridStyle}"
                                  ColumnHeaderStyle="{StaticResource DarkDataGridColumnHeaderStyle}"
                                  CellStyle="{StaticResource DarkDataGridCellStyle}"
                                  RowStyle="{StaticResource DarkDataGridRowStyle}"
                                  AlternationCount="2"
                                  MaxHeight="400">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Ürün" Binding="{Binding UrunAd}" Width="*"/>
                                <DataGridTextColumn Header="Kategori" Binding="{Binding KategoriAd}" Width="100"/>
                                <DataGridTextColumn Header="Adet" Binding="{Binding ToplamAdet}" Width="60"/>
                                <DataGridTextColumn Header="Birim Fiyat" Binding="{Binding BirimFiyat, StringFormat=₺{0:N2}}" Width="90"/>
                                <DataGridTextColumn Header="Toplam" Binding="{Binding ToplamTutar, StringFormat=₺{0:N2}}" Width="100"/>
                                <DataGridTextColumn Header="İkram" Binding="{Binding IkramAdet}" Width="60"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </StackPanel>
                
                <!-- Ödeme Tipi Raporu -->
                <StackPanel Visibility="{Binding SelectedRaporTipi, Converter={StaticResource StringEqualsVisibility}, ConverterParameter=Odeme}">
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="💳 Ödeme Tipi Raporu" FontSize="16" FontWeight="Bold" Foreground="#cdd6f4" Margin="0,0,0,15"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBlock Text="Başlangıç:" Foreground="#bac2de" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <DatePicker SelectedDate="{Binding BaslangicTarihi}" Width="130"/>
                                <TextBlock Text="Bitiş:" Foreground="#bac2de" VerticalAlignment="Center" Margin="15,0,10,0"/>
                                <DatePicker SelectedDate="{Binding BitisTarihi}" Width="130"/>
                                <Button Content="🔍 Rapor Getir" 
                                        Command="{Binding LoadOdemeTipleriCommand}"
                                        Style="{StaticResource ActionButtonStyle}"
                                        Margin="15,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <ItemsControl ItemsSource="{Binding OdemeTipleri}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource CardStyle}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding OdemeTipi}" Foreground="#cdd6f4" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" Text="{Binding AdisyonSayisi, StringFormat={}{0} adisyon}" Foreground="#bac2de" VerticalAlignment="Center" Margin="0,0,20,0"/>
                                        <TextBlock Grid.Column="2" Text="{Binding ToplamTutar, StringFormat=₺{0:N2}}" Foreground="#a6e3a1" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
                                        <Border Grid.Column="3" Background="#89b4fa" CornerRadius="5" Padding="8,4">
                                            <TextBlock Text="{Binding Yuzde, StringFormat=%{0:N1}}" Foreground="#1e1e2e" FontWeight="Bold"/>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
                
                <!-- Aylık Rapor -->
                <StackPanel Visibility="{Binding SelectedRaporTipi, Converter={StaticResource StringEqualsVisibility}, ConverterParameter=Aylik}">
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="📆 Aylık Rapor" FontSize="16" FontWeight="Bold" Foreground="#cdd6f4" Margin="0,0,0,15"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBlock Text="Yıl:" Foreground="#bac2de" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <ComboBox ItemsSource="{Binding Yillar}" 
                                          SelectedItem="{Binding SelectedYil}" 
                                          Width="90"
                                          Style="{StaticResource DarkComboBoxStyle}"
                                          ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}"/>
                                <TextBlock Text="Ay:" Foreground="#bac2de" VerticalAlignment="Center" Margin="15,0,10,0"/>
                                <ComboBox ItemsSource="{Binding Aylar}" 
                                          SelectedValue="{Binding SelectedAy}"
                                          SelectedValuePath="Ay"
                                          DisplayMemberPath="Ad"
                                          Width="120"
                                          Style="{StaticResource DarkComboBoxStyle}"
                                          ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}"/>
                                <Button Content="🔍 Rapor Getir" 
                                        Command="{Binding LoadAylikRaporCommand}"
                                        Style="{StaticResource ActionButtonStyle}"
                                        Margin="15,0,0,0"/>
                                <Button Content="📥 Excel'e Aktar" 
                                        Command="{Binding ExportExcelCommand}"
                                        Style="{StaticResource ActionButtonStyle}"
                                        Background="#f9e2af"
                                        Margin="10,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <!-- Aylık Özet -->
                    <Border Style="{StaticResource CardStyle}" Visibility="{Binding AylikRapor, Converter={StaticResource NullToVisibility}}">
                        <StackPanel>
                            <TextBlock FontSize="18" FontWeight="Bold" Foreground="#cdd6f4" Margin="0,0,0,15">
                                <Run Text="{Binding AylikRapor.AyAdi}"/>
                                <Run Text=" "/>
                                <Run Text="{Binding AylikRapor.Yil}"/>
                                <Run Text=" Özeti"/>
                            </TextBlock>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Border Grid.Column="0" Background="#45475a" CornerRadius="8" Padding="15" Margin="5">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="Toplam Adisyon" Foreground="#bac2de" FontSize="12"/>
                                        <TextBlock Text="{Binding AylikRapor.ToplamAdisyon}" Foreground="#a6e3a1" FontSize="24" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="1" Background="#45475a" CornerRadius="8" Padding="15" Margin="5">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="Brüt Tutar" Foreground="#bac2de" FontSize="12"/>
                                        <TextBlock Text="{Binding AylikRapor.ToplamTutar, StringFormat=₺{0:N2}}" Foreground="#89b4fa" FontSize="24" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Background="#45475a" CornerRadius="8" Padding="15" Margin="5">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="İndirimler" Foreground="#bac2de" FontSize="12"/>
                                        <TextBlock Text="{Binding AylikRapor.ToplamIndirim, StringFormat=₺{0:N2}}" Foreground="#f38ba8" FontSize="24" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="3" Background="#45475a" CornerRadius="8" Padding="15" Margin="5">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="Net Tutar" Foreground="#bac2de" FontSize="12"/>
                                        <TextBlock Text="{Binding AylikRapor.NetTutar, StringFormat=₺{0:N2}}" Foreground="#f9e2af" FontSize="24" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                            
                            <!-- Günlük Grafiği (Basit liste) -->
                            <TextBlock Text="Günlük Satışlar" Foreground="#cdd6f4" FontSize="14" FontWeight="SemiBold" Margin="0,20,0,10"/>
                            <ItemsControl ItemsSource="{Binding AylikRapor.GunlukOzetler}" MaxHeight="200">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <VirtualizingStackPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="80"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Tarih, StringFormat=dd.MM.yyyy}" Foreground="#bac2de" FontSize="11"/>
                                            <ProgressBar Grid.Column="1" Value="{Binding ToplamTutar}" Maximum="{Binding DataContext.AylikRapor.ToplamTutar, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                         Height="12" Background="#45475a" Foreground="#89b4fa" Margin="5,0"/>
                                            <TextBlock Grid.Column="2" Text="{Binding ToplamTutar, StringFormat=₺{0:N0}}" Foreground="#a6e3a1" FontSize="11" HorizontalAlignment="Right"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
